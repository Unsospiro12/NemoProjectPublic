
public enum CurrencyType
{
    Gold,
    Diamond
}

public enum SelectableType
{
    UserUnit,
    Structure,
    Enemy
}


public enum UnitName
{
    Archer,
    Infantry,
    Barbarian,
    Assassin,
    Enemy,
}

public enum UserColor
{
    Red,
    Blue
}

public enum UnitType
{
    Melee,
    Range,
    Magic
}

public enum RewardType
{
    Round,
    Kill,
    Gamble
}

public enum StatusEffectType
{
    Slow = 1,
    Bleed
}

public enum ProjectileNames
{
    Arrow,
    FireBalt,
    FasterArrow,
    BiggerFireBalt,
    SkillFireBalt
}

public enum TextVariety
{
    CurrencyText,
    UnitText,
}

public enum Speaker
{
    Guide,
    User,
    Enemy
}

public enum UnitDefType
{
    LightArmor,
    HeavyArmor
}

public enum TutorialScreen
{
    Gamble,
    Unit,
    Upgrade,
    Control
}

public enum StatType
{
    Damage,
    AttackSpeed,
    MovementSpeed,
    AttackRange
}

public enum SkillEffect
{
    ScorchedFloor
}

public enum SoundType
{
    BGM,
    SFX
}

public enum BGMList
{
    MainMenu,
    HomeTown,
    OverworldChorus,
    overworldNoChorus,
    Forest,
    Farm,
    Battle,
    BossBattle,
    Catacombs,
    Victory,
    AncientCave,
    FinalBattle,
    CreditsOneShot,
    CreditsLoop
}

public enum Notification
{
    Gold,
    Diamond,
    Area,
}